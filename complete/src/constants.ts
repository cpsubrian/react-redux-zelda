import {LayerName} from './types';

export const CELL_SIZE = 16;
export const MAP_WIDTH = CELL_SIZE * 36;
export const MAP_HEIGHT = CELL_SIZE * 28;
export const LAYERS: Array<LayerName> = ['terrain', 'objects'];
